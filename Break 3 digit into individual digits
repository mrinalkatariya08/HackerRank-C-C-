#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   
    int n;
    scanf("%d", &n);

    int A = n / 100;          
    int B = (n / 10) % 10;    
    int C = n % 10;           

    printf("%d %d %d", A, B, C);
    
    return 0;
}
